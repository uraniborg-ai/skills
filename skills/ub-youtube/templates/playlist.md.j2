---
title: "{{ title }}"
source_url: "{{ source_url }}"
source_kind: "{{ source_kind }}"
fetched_at: "{{ fetched_at }}"
playlist_id: "{{ playlist_id }}"
channel: "{{ channel }}"
total_videos: {{ total_videos }}
total_duration: "{{ total_duration }}"
with_transcripts: {{ "true" if with_transcripts else "false" }}
transcript_limit: {{ transcript_limit if transcript_limit is not none else "null" }}
---

# {{ title }}

Source: {{ source_url }}

## Metadata

- Source kind: {{ source_kind }}
- Channel: {{ channel or "N/A" }}
- Fetched at: {{ fetched_at }}
- Total videos: {{ total_videos }}
- Total duration: {{ total_duration }}
- Transcripts: {% if with_transcripts %}requested, limit {{ transcript_limit }}{% else %}not requested{% endif %}

## Videos

{% for video in videos %}
### {{ loop.index }}. {{ video.title }}

- Channel: {{ video.channel or "N/A" }}
- Duration: {{ video.duration_formatted }}
- Upload date: {{ video.upload_date or "N/A" }}
- URL: {{ video.url or "N/A" }}
- Video ID: {{ video.video_id or "N/A" }}
- Transcript status: {{ video.transcript_status }}{% if video.transcript_path %}
- Transcript path: {{ video.transcript_path }}{% endif %}{% if video.transcript_error %}
- Transcript error: {{ video.transcript_error }}{% endif %}

{% endfor %}
