[project]
name = "uraniborg-skills-dev"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
  "ruff>=0.8",
]

[tool.ruff]
line-length = 100
target-version = "py311"

